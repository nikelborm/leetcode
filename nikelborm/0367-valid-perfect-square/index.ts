// https://leetcode.com/problems/valid-perfect-square/description/

import { deepEqual, equal } from 'assert';
import { it } from 'node:test';

function isPerfectSquare(num: number): boolean {
  while(false) {

  }

  return false;
};

it('case 1', () => {
  equal(isPerfectSquare(16), true);
})

it('case 2', () => {
  equal(isPerfectSquare(16), true);
})
